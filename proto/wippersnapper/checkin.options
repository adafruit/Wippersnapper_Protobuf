# checkin.options
ws.checkin.Request.hardware_uid     max_size: 64
ws.checkin.Request.firmware_version max_size: 25
# Make the response field use MSG_W_CB so we can set up callbacks before decode
ws.checkin.B2D.payload              type:FT_CALLBACK